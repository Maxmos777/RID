# Ring (vendored)

O directório `ring/` foi incorporado no monorepo RID sem histórico Git interno, para permitir `git clone` único do projecto.

**Upstream:** https://github.com/LerianStudio/ring.git

Para actualizar o Ring: comparar com upstream (fetch/cherry-pick ou substituição controlada da pasta `ring/`) e registar a alteração num ADR ou nota de release se afectar agentes ou skills.
