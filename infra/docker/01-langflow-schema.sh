#!/bin/sh
# DEPRECATED — substituído por fonte única em Django:
#   docker compose run --rm langflow-pg-bootstrap
#   (perfil langflow) ou: python manage.py ensure_langflow_schema
#
# Mantido vazio para não quebrar mounts antigos; removido do docker-compose RID.
exit 0
