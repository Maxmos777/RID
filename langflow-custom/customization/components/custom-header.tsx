// Ponto de extensão: header customizado RID.
// Retorna vazio por enquanto — o header padrão do Langflow é usado.
// Substituir pelo componente de branding RID quando o design estiver definido.
export function CustomHeader() {
  return <></>;
}
