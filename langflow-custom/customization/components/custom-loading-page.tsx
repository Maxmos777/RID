// Ponto de extensão: loading screen com branding RID.
// Retorna vazio por enquanto — usa o loading padrão do Langflow.
// Substituir com spinner/logo RID quando o design estiver definido.
export function CustomLoadingPage() {
  return <></>;
}
