// Ponto de extensão: headers HTTP adicionados a cada request Langflow.
// Quando necessário, injectar X-Tenant-Schema ou tokens RID aqui.
export function useCustomApiHeaders(): Record<string, string> {
  return {};
}
